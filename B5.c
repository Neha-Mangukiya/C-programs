#include<stdio.h>
main()
{
	float base,height,area;
	printf("enter the value of base:");
	scanf("%f",&base);
	printf("\nenter the valve of height:");
	scanf("%f",&height);
	area=(base*height)/2;
	printf("\narea of triangle=%f",area);
}
