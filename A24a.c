//Write a Program of Print a number and check the number is palindrome or not using recursive Function
#include<stdio.h>
int main()
{
	char st1[10],st2[10];
	int flag,i=0,j;
	printf("enter the number:");
	scanf("%d",&st1);
	printf("\n reverse = %s",strrev(st1));
}